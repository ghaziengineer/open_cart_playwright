{
  "name": "open_cart_playwright",
  "version": "1.0.0",
  "description": "```bash\r #Initialiser un projet Node.js\r npm init -y\r #Installer Playwright avec TypeScript\r npm install -D @playwright/test\r npx playwright install\r #Initialiser la structure Playwright (création automatique des fichiers de base)\r #configuration TypeScript\r npm install typescript\r npx tsc --init\r ```",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
